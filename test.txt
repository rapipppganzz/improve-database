-- Script Penghapus Objek FE Bypass (EXPERIMENTAL)
-- WARNING: Kemungkinan besar TIDAK WORK di kebanyakan game modern
-- Hanya work jika game memiliki vulnerability tertentu

local Players = game:GetService("Players")
local player = Players.LocalPlayer
local mouse = player:GetMouse()

print("=== FE Bypass Click Delete (EXPERIMENTAL) ===")
print("WARNING: Mungkin tidak work di game dengan FE strict!")

local isEnabled = false

-- Coba ambil network ownership (method 1)
local function takeOwnership(obj)
    pcall(function()
        if obj:IsA("BasePart") then
            obj.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
            obj.AssemblyAngularVelocity = Vector3.new(0, 0, 0)
            obj:SetNetworkOwner(player)
        end
    end)
end

-- Method 2: Anchor manipulation
local function anchorExploit(obj)
    pcall(function()
        if obj:IsA("BasePart") and not obj.Anchored then
            obj.Anchored = false
            obj.CFrame = obj.CFrame * CFrame.new(0, -999999, 0) -- Kirim ke void
        end
    end)
end

-- Method 3: Weld destruction (untuk objek yang di-weld)
local function destroyWelds(obj)
    pcall(function()
        for _, weld in pairs(obj:GetDescendants()) do
            if weld:IsA("Weld") or weld:IsA("WeldConstraint") or weld:IsA("Motor6D") then
                weld:Destroy()
            end
        end
    end)
end

-- Method 4: Transparency + CanCollide (visual hide)
local function hideObject(obj)
    pcall(function()
        if obj:IsA("BasePart") then
            obj.Transparency = 1
            obj.CanCollide = false
            for _, child in pairs(obj:GetDescendants()) do
                if child:IsA("BasePart") then
                    child.Transparency = 1
                    child.CanCollide = false
                end
                if child:IsA("Decal") or child:IsA("Texture") then
                    child.Transparency = 1
                end
            end
        end
    end)
end

-- Method 5: RemoteEvent spam (kalau ada vulnerability)
local function tryRemoteDelete(obj)
    pcall(function()
        for _, remote in pairs(game:GetDescendants()) do
            if remote:IsA("RemoteEvent") and (string.find(remote.Name:lower(), "delete") or string.find(remote.Name:lower(), "remove")) then
                remote:FireServer(obj)
            end
        end
    end)
end

-- Buat GUI
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "FEDeleteGUI"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = player.PlayerGui

local Frame = Instance.new("Frame")
Frame.Size = UDim2.new(0, 220, 0, 100)
Frame.Position = UDim2.new(0, 10, 0.5, -50)
Frame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
Frame.BorderSizePixel = 0
Frame.Parent = ScreenGui

local UICorner = Instance.new("UICorner")
UICorner.CornerRadius = UDim.new(0, 8)
UICorner.Parent = Frame

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, 0, 0, 35)
Title.BackgroundTransparency = 1
Title.Text = "FE Delete (EXP)"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 14
Title.Font = Enum.Font.GothamBold
Title.Parent = Frame

local Warning = Instance.new("TextLabel")
Warning.Size = UDim2.new(1, 0, 0, 20)
Warning.Position = UDim2.new(0, 0, 0, 30)
Warning.BackgroundTransparency = 1
Warning.Text = "Mungkin tidak work!"
Warning.TextColor3 = Color3.fromRGB(255, 200, 50)
Warning.TextSize = 10
Warning.Font = Enum.Font.Gotham
Warning.Parent = Frame

local ToggleButton = Instance.new("TextButton")
ToggleButton.Size = UDim2.new(0, 190, 0, 35)
ToggleButton.Position = UDim2.new(0, 15, 0, 55)
ToggleButton.BackgroundColor3 = Color3.fromRGB(220, 50, 50)
ToggleButton.Text = "OFF"
ToggleButton.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleButton.TextSize = 16
ToggleButton.Font = Enum.Font.GothamBold
ToggleButton.Parent = Frame

local ButtonCorner = Instance.new("UICorner")
ButtonCorner.CornerRadius = UDim.new(0, 6)
ButtonCorner.Parent = ToggleButton

-- Toggle
ToggleButton.MouseButton1Click:Connect(function()
    isEnabled = not isEnabled
    
    if isEnabled then
        ToggleButton.Text = "ON - Klik Objek"
        ToggleButton.BackgroundColor3 = Color3.fromRGB(50, 220, 50)
        print("FE Delete: ON")
    else
        ToggleButton.Text = "OFF"
        ToggleButton.BackgroundColor3 = Color3.fromRGB(220, 50, 50)
        print("FE Delete: OFF")
    end
end)

-- Click handler dengan multiple methods
mouse.Button1Down:Connect(function()
    if not isEnabled then return end
    
    local target = mouse.Target
    
    if target then
        -- Cek bukan player
        local isPlayerPart = false
        local ancestor = target
        
        while ancestor do
            if ancestor:IsA("Model") and Players:GetPlayerFromCharacter(ancestor) then
                isPlayerPart = true
                break
            end
            ancestor = ancestor.Parent
        end
        
        if not isPlayerPart then
            local objName = target.Name
            print("Mencoba hapus: " .. objName)
            
            -- Coba semua method
            takeOwnership(target) -- Method 1
            wait(0.1)
            destroyWelds(target) -- Method 3
            wait(0.1)
            anchorExploit(target) -- Method 2
            hideObject(target) -- Method 4 (fallback visual)
            tryRemoteDelete(target) -- Method 5
            
            -- Client-side delete sebagai fallback
            pcall(function()
                target:Destroy()
            end)
            
            print("Proses selesai untuk: " .. objName)
            
            game.StarterGui:SetCore("SendNotification", {
                Title = "FE Delete";
                Text = "Mencoba hapus " .. objName;
                Duration = 2;
            })
        end
    end
end)

-- Draggable
local dragging, dragInput, dragStart, startPos

Frame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true
        dragStart = input.Position
        startPos = Frame.Position
    end
end)

Frame.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement then
        dragInput = input
    end
end)

Frame.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = false
    end
end)

game:GetService("UserInputService").InputChanged:Connect(function(input)
    if input == dragInput and dragging then
        local delta = input.Position - dragStart
        Frame.Position = UDim2.new(
            startPos.X.Scale,
            startPos.X.Offset + delta.X,
            startPos.Y.Scale,
            startPos.Y.Offset + delta.Y
        )
    end
end)

print("FE Bypass loaded! Note: Ini SANGAT tergantung game!")
print("Kalau tidak work, artinya game punya FE protection ketat.")
